'use strict';

const getPlace = require('./getPlace');
const listPlaces = require('./listPlaces');
const newPlace = require('./newPlace');
const editPlace = require('./editPlace');

module.exports = { getPlace, listPlaces, newPlace, editPlace };
